// Roadmap Configuration
// Only change this value to update the status across all languages
export const roadmapConfig = {
  currentPhase: 1, // 1 = Phase 1 (Genesis), 2 = Phase 2, etc.
  showStatus: true, // Show the global warning banner
};

